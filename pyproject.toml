[project]
name = "growth-knowledge-hub"
version = "0.1.0"
description = "AI CLI skill for local personal growth knowledge memory"
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
